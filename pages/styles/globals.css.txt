@tailwind base;
@tailwind components;
@tailwind utilities;

/* Ensure full height and pale blue gradient across body and root */
html, body, #__next {
  height: 100%;
  margin: 0;
  padding: 0;
  background: linear-gradient(to bottom, #eff8ff 0%, #dbeafe 40%, #bfdbfe 100%);
  font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}

/* Slightly translucent white for backgrounds to let gradient show through */
.bg-white {
  background-color: rgba(255, 255, 255, 0.92) !important;
}

/* Solid white helper class if needed */
.bg-solid-white {
  background-color: #ffffff !important;
}

/* Ensure DataTable headers and rows have subtle shadows for readability */
.rd-table {
  background-color: rgba(255,255,255,0.92) !important;
}

.rd-table th, .rd-table td {
  border-color: #e5e7eb; /* Tailwind gray-200 */
}

/* Optional: smooth font rendering for all text */
body {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
