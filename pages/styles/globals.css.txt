@tailwind base;
@tailwind components;
@tailwind utilities;

/* Ensure body-level pale blue gradient */
html, body, #__next {
  height: 100%;
  margin: 0;
  padding: 0;
  background: linear-gradient(to bottom, #eff8ff 0%, #dbeafe 40%, #bfdbfe 100%);
  font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}

/* Make bg-white slightly translucent so gradient shows through */
.bg-white {
  background-color: rgba(255,255,255,0.92) !important;
}

/* Solid white helper if you really need it */
.bg-solid-white { background-color: #ffffff !important; }
